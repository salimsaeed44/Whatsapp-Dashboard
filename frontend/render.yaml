# Render Static Site Configuration
# This file configures routing for React Router on Render

services:
  - type: web
    name: whatsapp-dashboard-frontend
    env: static
    buildCommand: npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
